# 安全策略 · Security Policy

> **YanYuCloudCube™** | **言启象限 · 语枢未来**
>
> *万象归元于云枢 | 深栈智启新纪元*

---

## 🛡️ 安全承诺

YYC³ AI-Family 秉承 **"数据主权在用户"** 的安全理念：

- **零上传**：所有数据仅存储于用户本地设备
- **零收集**：不收集任何用户信息
- **零追踪**：不追踪用户行为
- **完全开源**：代码透明可审计
- **用户主权**：数据完全由用户控制

## 🔐 安全架构

### 三层安全体系

```
┌───────────────────────────────────────────────────────────┐
│                  YYC³ AI-Family 安全体系                    │
├───────────────────────────────────────────────────────────┤
│                                                           │
│   1. 传输安全                                              │
│   ├── WebSocket WSS (生产环境)                            │
│   ├── CORS 严格策略                                        │
│   └── API 请求来源验证                                     │
│                                                           │
│   2. 存储安全                                              │
│   ├── Web Crypto API AES-GCM 256-bit 加密                  │
│   ├── localStorage + IndexedDB 沙箱隔离                    │
│   └── NAS SQLite 本地网络隔离                              │
│                                                           │
│   3. 应用安全                                              │
│   ├── Sentiniel 智能体行为审计                             │
│   ├── 5 级安全级别可视化                                   │
│   ├── CSP 内容安全策略                                     │
│   └── XSS 防护 (React 默认转义)                            │
│                                                           │
└───────────────────────────────────────────────────────────┘
```

## 📢 报告漏洞

如果您发现了安全漏洞，**请不要公开披露**，而是通过以下方式私下报告：

### 报告流程

1. **发送邮件**：`admin@0379.email`
2. **邮件主题**：`[SECURITY] YYC3-AF 安全漏洞报告`
3. **邮件内容**：
   - 漏洞描述
   - 复现步骤
   - 影响范围
   - 可能的修复建议

### 预期响应时间

| 阶段 | 时间 |
|------|------|
| 确认收到 | 24 小时内 |
| 初步评估 | 48 小时内 |
| 修复计划 | 7 天内 |
| 发布修复 | 根据严重程度 |

## 🏷️ 版本支持

| 版本 | 支持状态 |
|------|---------|
| 0.35.x | ✅ 全面支持 |
| 0.34.x | 🔒 安全更新 |
| < 0.34 | ❌ 不再支持 |

## 🧪 已知安全措施

- ✅ **Web Crypto API**：AES-GCM 256-bit 本地加密
- ✅ **Content Security Policy**：严格的 CSP 头
- ✅ **HTTPS 优先**：生产环境强制 HTTPS
- ✅ **依赖审计**：`pnpm audit` 定期检查
- ✅ **代码审查**：所有 PR 必须经过 Code Review
- ✅ **最小权限**：仅请求必要浏览器权限

## 🔒 安全配置建议

### 生产环境

```nginx
# CSP 配置示例
Content-Security-Policy: 
  default-src 'self';
  script-src 'self';
  style-src 'self' 'unsafe-inline';
  connect-src 'self' ws://*.local;
  img-src 'self' data:;
  font-src 'self';
```

### 开发环境

```bash
# 定期审计依赖
pnpm audit

# 更新依赖
pnpm update

# 运行安全测试
pnpm test:e2e
```

## 📝 安全更新日志

安全更新将记录在 [CHANGELOG.md](CHANGELOG.md) 中，标记为 `🔒 SECURITY`。

---

> **YanYuCloudCube™** | **言启象限 · 语枢未来**
>
> *万象归元于云枢 | 深栈智启新纪元*
