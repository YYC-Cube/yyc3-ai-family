# YYC³ AI-Family 4 环境部署指南

## 📋 环境配置总览

| 环境 | 端口 | 命令 | 用途 |
|------|------|------|------|
| **Environment 1** (Development) | 3113 | `bun run dev` 或 `bun run dev:1` | 日常开发，快速迭代 |
| **Environment 2** (Staging) | 3114 | `bun run dev:2` 或 `bun run dev:staging` | 预发布，模拟生产 |
| **Environment 3** (Production) | 3115 | `bun run build && bun run preview` | 生产环境，稳定优先 |
| **Environment 4** (Testing) | 3116 | `bun run dev:4` 或 `bun run dev:testing` | 测试，激进设置 |

## 🚀 快速启动

### 方式 1: 启动单个环境

```bash
# 环境配置
bun run dev          # 启动环境 1 (端口 3113)
bun run dev:2        # 启动环境 2 (端口 3114)
bun run dev:3        # 构建并预览环境 3 (端口 3115)
bun run dev:4        # 启动环境 4 (端口 3116)
```

### 方式 2: 同时启动多个环境

在不同终端中运行：

```bash
# 终端 1: 开发环境
bun run dev

# 终端 2: 预发布环境
bun run dev:staging

# 终端 3: 测试环境
bun run dev:testing
```

## 🧠 AI 能力配置对比

| 配置项 | 环境配置 (1) | 预发布 (2) | 生产 (3) | 测试 (4) |
|--------|-------------|-----------|---------|---------|
| **上下文窗口** | 4096 tokens | 8192 tokens | 6144 tokens | 16384 tokens |
| **温度参数** | 0.5 | 0.7 | 0.6 | 0.9 |
| **流式输出** | ✅ | ✅ | ✅ | ✅ |
| **最大重试** | 2 次 | 3 次 | 3 次 | 5 次 |
| **超时时间** | 30 秒 | 60 秒 | 45 秒 | 120 秒 |
| **函数调用** | ✅ | ✅ | ✅ | ✅ |

## 📚 知识库配置对比

| 配置项 | 环境配置 (1) | 预发布 (2) | 生产 (3) | 测试 (4) |
|--------|-------------|-----------|---------|---------|
| **相似度阈值** | 0.80 | 0.75 | 0.85 | 0.50 |
| **最大结果数** | 5 | 10 | 8 | 20 |
| **语义缓存** | ❌ | ✅ | ✅ | ✅ |

## 🗂️ 持久化策略

| 环境 | 策略 | 说明 |
|------|------|------|
| 环境配置 (1) | localStorage | 浏览器本地存储，快速开发 |
| 预发布 (2) | localStorage | 混合测试 |
| 生产 (3) | nasSqlite | NAS 持久化，稳定可靠 |
| 测试 (4) | localStorage | 轻量测试 |

## 📊 日志级别

| 环境 | 日志级别 | 用途 |
|------|----------|------|
| 环境配置 (1) | debug | 详细调试信息 |
| 预发布 (2) | info | 常规信息 |
| 生产 (3) | warn | 仅警告和错误 |
| 测试 (4) | debug | 完整调试日志 |

## 🔧 端口冲突避免

所有端口使用 **3113 系列**，避免与常见服务冲突：

- **3113** - 开发环境
- **3114** - 预发布环境
- **3115** - 生产环境
- **3116** - 测试环境

## 🌐 访问地址

启动后在浏览器中访问：

```
http://localhost:3113  # 开发环境
http://localhost:3114  # 预发布环境
http://localhost:3115  # 生产环境
http://localhost:3116  # 测试环境
```

## 📦 构建命令

```bash
# 构建生产版本
bun run build

# 构建预发布版本
bun run build:staging

# 构建测试版本
bun run build:testing

# 预览生产构建
bun run preview          # 端口 3115
bun run preview:staging  # 端口 3114
bun run preview:testing  # 端口 3116
```

## 🎯 使用场景

### 场景 1: 日常开发
```bash
bun run dev
# 访问 http://localhost:3113
# 快速迭代，debug 日志，localStorage
```

### 场景 2: 新功能预发布
```bash
bun run dev:staging
# 访问 http://localhost:3114
# 模拟生产配置，AI 能力增强
```

### 场景 3: 生产部署
```bash
bun run build && bun run preview
# 访问 http://localhost:3115
# NAS 持久化，稳定配置
```

### 场景 4: AI 功能测试
```bash
bun run dev:testing
# 访问 http://localhost:3116
# 最大上下文，激进设置，完整日志
```

## 🔄 环境切换

在不同环境间切换时，浏览器会自动加载对应环境配置。如需清除缓存：

```bash
# 清除 localStorage
# 在浏览器控制台运行：
localStorage.clear()
location.reload()
```

## 🛠️ 故障排查

### 端口占用
```bash
# 检查端口占用
lsof -i :3113
lsof -i :3114
lsof -i :3115
lsof -i :3116

# 终止进程
kill -9 <PID>
```

### 环境变量未生效
确保 `.env.[environment]` 文件存在且格式正确：
- `.env.development` (环境 1)
- `.env.staging` (环境 2)
- `.env.production` (环境 3)
- `.env.testing` (环境 4)

### 热重载不工作
重启开发服务器：
```bash
# Ctrl+C 停止
bun run dev  # 重新启动
```

## 📝 注意事项

1. **首次启动**：运行 `bun install` 安装依赖
2. **API 密钥**：在各环境的 UI 设置中配置 LLM 提供商密钥
3. **NAS 连接**：确保生产环境 (环境 3) 可访问 NAS (192.168.3.45)
4. **类型检查**：运行 `bun run type-check` 确保 TypeScript 无错误
5. **测试**：运行 `bun run test` 确保 62/62 测试通过

---

**更新日期**: 2026-02-17
**版本**: 0.33.0
