# YYC³ AI Family - 项目启动测试清单

> ***YanYuCloudCube***
> 言启象限 | 语枢未来

---

**文档版本**: 1.0.0
**创建日期**: 2026-02-22
**用途**: 项目启动前的全面测试验证

---

## 📋 测试清单

### Phase 1: 基础设施测试

#### 1.1 网络连接

| 测试项 | 命令 | 预期结果 | 状态 |
|--------|------|----------|------|
| NAS SSH | `nc -z -w 3 192.168.3.45 9557` | 连接成功 | ⬜ |
| NAS Web | `curl -s http://192.168.3.45:9898` | 返回 HTML | ⬜ |
| iMac SSH | `nc -z -w 3 192.168.3.77 22` | 连接成功 | ⬜ |
| iMac Ollama | `curl -s http://192.168.3.77:11434/api/version` | 返回版本信息 | ⬜ |

#### 1.2 数据库服务

| 测试项 | 命令 | 预期结果 | 状态 |
|--------|------|----------|------|
| PostgreSQL 本地 | `psql -h localhost -p 5433 -U yyc3 -d yyc3_aify -c "SELECT 1"` | 返回 1 | ⬜ |
| pgvector NAS | `PGPASSWORD=yyc3_vector_2026 psql -h 192.168.3.45 -p 5434 -U yyc3 -d yyc3_vectors -c "SELECT 1"` | 返回 1 | ⬜ |
| Redis | `redis-cli ping` | 返回 PONG | ⬜ |

#### 1.3 AI 模型服务

| 测试项 | 命令 | 预期结果 | 状态 |
|--------|------|----------|------|
| Ollama M4 Max | `curl -s http://localhost:11434/api/tags` | 返回模型列表 | ⬜ |
| Ollama iMac | `curl -s http://192.168.3.77:11434/api/tags` | 返回模型列表 | ⬜ |
| 模型推理测试 | `ollama run qwen2.5:7b "你好"` | 返回回复 | ⬜ |
| 嵌入向量测试 | `ollama run nomic-embed-text "测试"` | 返回向量 | ⬜ |

---

### Phase 2: 应用服务测试

#### 2.1 前端服务

```bash
# 启动前端
cd /Users/yanyu/YYC3-Mac-Max/Family-π³/AI-Family
pnpm dev

# 访问地址
open http://localhost:3200
```

| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 页面加载 | 显示 AI Family 界面 | ⬜ |
| 无控制台错误 | Console 无红色错误 | ⬜ |
| 网络请求正常 | Network 面板请求成功 | ⬜ |

#### 2.2 API 服务

```bash
# 启动 API
cd /Users/yanyu/YYC3-Mac-Max/Family-π³/API
pnpm dev

# 健康检查
curl http://localhost:3210/health
```

| 测试项 | 预期结果 | 状态 |
|--------|----------|------|
| 健康检查 | 返回 {"status": "ok"} | ⬜ |
| API 文档 | 可访问 /api-docs | ⬜ |
| CORS 配置 | 跨域请求正常 | ⬜ |

---

### Phase 3: 集成测试

#### 3.1 数据库集成

```bash
# 测试 PostgreSQL 连接
psql -h localhost -p 5433 -U yyc3 -d yyc3_aify -c "
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public';
"

# 测试 pgvector
PGPASSWORD=yyc3_vector_2026 psql -h 192.168.3.45 -p 5434 -U yyc3 -d yyc3_vectors -c "
SELECT extname, extversion FROM pg_extension WHERE extname = 'vector';
"
```

#### 3.2 AI 模型集成

```bash
# 测试本地模型
curl -X POST http://localhost:11434/api/generate -H "Content-Type: application/json" -d '{
  "model": "qwen2.5:7b",
  "prompt": "测试",
  "stream": false
}'

# 测试 iMac 模型
curl -X POST http://192.168.3.77:11434/api/generate -H "Content-Type: application/json" -d '{
  "model": "phi3:mini",
  "prompt": "测试",
  "stream": false
}'
```

#### 3.3 向量数据库集成

```bash
# 测试向量搜索
PGPASSWORD=yyc3_vector_2026 psql -h 192.168.3.45 -p 5434 -U yyc3 -d yyc3_vectors -c "
SELECT id, content, 
       1 - (embedding <=> array_fill(0.1, ARRAY[768])::vector(768)) as similarity 
FROM documents 
ORDER BY embedding <=> array_fill(0.1, ARRAY[768])::vector(768) 
LIMIT 3;
"
```

---

### Phase 4: 功能测试

#### 4.1 智能体测试

| 智能体 | 模型 | 测试命令 | 状态 |
|--------|------|----------|------|
| Navigator | qwen2.5:7b | `ollama run qwen2.5:7b "导航测试"` | ⬜ |
| Thinker | DeepSeek API | API 调用测试 | ⬜ |
| Prophet | qwen2.5:7b | `ollama run qwen2.5:7b "预测测试"` | ⬜ |
| Bole | codegeex4 | `curl http://192.168.3.77:11434/api/generate -d '{"model":"codegeex4","prompt":"代码评估"}'` | ⬜ |
| Sentinel | phi3:mini | `curl http://192.168.3.77:11434/api/generate -d '{"model":"phi3:mini","prompt":"安全检查"}'` | ⬜ |
| Pivot | glm4:9b | `curl http://192.168.3.77:11434/api/generate -d '{"model":"glm4:9b","prompt":"协调测试"}'` | ⬜ |
| Grandmaster | DeepSeek API | API 调用测试 | ⬜ |

#### 4.2 端到端测试

| 测试场景 | 步骤 | 预期结果 | 状态 |
|----------|------|----------|------|
| 用户对话 | 发送消息 → 接收回复 | 正常对话流程 | ⬜ |
| 知识检索 | 查询知识库 → 返回结果 | 向量搜索正常 | ⬜ |
| 代码生成 | 输入需求 → 生成代码 | CodeGeeX4 响应正常 | ⬜ |

---

## 🚀 启动流程

### 快速启动命令

```bash
# 1. 运行启动测试
cd /Users/yanyu/YYC3-Mac-Max/Family-π³
chmod +x scripts/startup-test.sh
./scripts/startup-test.sh

# 2. 启动前端 (新终端)
cd /Users/yanyu/YYC3-Mac-Max/Family-π³/AI-Family
pnpm dev

# 3. 启动 API (新终端)
cd /Users/yanyu/YYC3-Mac-Max/Family-π³/API
pnpm dev

# 4. 访问应用
open http://localhost:3200
```

### 服务启动顺序

```
1. 数据库服务
   └── PostgreSQL (已启动)
   └── Redis (如需要)
   
2. AI 模型服务
   └── Ollama (已启动)
   
3. 应用服务
   └── API Server
   └── Frontend
```

---

## ⚠️ 故障排查

### 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| PostgreSQL 连接失败 | 服务未启动 | `pg_ctl -D /usr/local/var/postgres start` |
| Ollama 无响应 | 服务未启动 | `ollama serve` |
| 前端无法访问 API | CORS 配置 | 检查 API CORS 设置 |
| 模型加载失败 | 磁盘空间不足 | `df -h ~/.ollama` |
| 向量搜索失败 | pgvector 未安装 | 检查扩展 `SELECT * FROM pg_extension` |

### 日志查看

```bash
# PostgreSQL 日志
tail -f /usr/local/var/log/postgres.log

# Ollama 日志
tail -f ~/.ollama/logs/server.log

# 前端日志
# 浏览器控制台 F12

# API 日志
# 终端输出
```

---

## ✅ 测试通过标准

| 级别 | 标准 |
|------|------|
| **关键** | 所有数据库、AI 模型服务正常 |
| **重要** | 前端、API 服务正常启动 |
| **一般** | 所有集成测试通过 |
| **完整** | 端到端功能测试通过 |

---

<div align="center">

**YYC³ AI Family**

*言启象限 | 语枢未来*

**万象归元于云枢 | 深栈智启新纪元**

---

*文档版本: 1.0.0*
*最后更新: 2026-02-22*

</div>
