# 🚀 YYC³ AI-Family 部署指南

## 📋 快速部署

### GitHub Pages 部署（推荐 - 完全免费）

#### 步骤 1: 创建 GitHub 仓库

1. 访问: https://github.com/new
2. 仓库名称: `yyc3-ai-family`（必须与 vite.config.ts 中的 base 路径一致）
3. 设置为 **Public**（开源精神）
4. **不要**初始化 README、.gitignore、license（我们已经有了）

#### 步骤 2: 推送代码到 GitHub

```bash
# 替换 YOUR_USERNAME 为你的 GitHub 用户名
git remote add origin https://github.com/YOUR_USERNAME/yyc3-ai-family.git
git branch -M main
git push -u origin main
```

#### 步骤 3: 启用 GitHub Pages

1. 访问仓库的 **Settings** → **Pages**
2. **Source** 选择: **GitHub Actions**
3. 保存设置

#### 步骤 4: 自动部署

推送代码后，GitHub Actions 会自动：
- 安装依赖
- 构建项目
- 部署到 GitHub Pages

#### 步骤 5: 访问你的网站

部署完成后（约 1-2 分钟），访问：
```
https://YOUR_USERNAME.github.io/yyc3-ai-family/
```

---

## 🔧 本地 4 环境部署

详细说明请查看：[docs/4-ENVIRONMENT-GUIDE.md](docs/4-ENVIRONMENT-GUIDE.md)

| 环境 | 端口 | 命令 | 用途 |
|------|------|------|------|
| 环境配置 (1) | 3113 | `bun run dev` | 日常开发 |
| 预发布 (2) | 3114 | `bun run dev:2` | 模拟生产 |
| 生产 (3) | 3115 | `bun run dev:3` | 稳定优先 |
| 测试 (4) | 3116 | `bun run dev:4` | 激进测试 |

---

## 🌐 其他部署方案

### Netlify（推荐 - 免费额度大）

1. 访问: https://app.netlify.com/drop
2. 拖拽 `dist/` 文件夹到页面
3. 获得一个 `*.netlify.app` 域名

### Vercel（推荐 - 速度快）

1. 访问: https://vercel.com/new
2. 导入 GitHub 仓库
3. 自动检测 Vite 配置
4. 一键部署

---

## 📦 部署前检查清单

- [ ] 代码已推送到 GitHub
- [ ] 所有依赖已安装 (`bun install`)
- [ ] 本地构建成功 (`bun run build`)
- [ ] TypeScript 无错误（可选，建议修复）
- [ ] 环境变量配置正确（如需要）

---

## 🔄 持续部署

配置完成后，每次推送 `main` 分支都会自动触发部署：

```bash
git add .
git commit -m "feat: 新功能"
git push
# 自动部署到 GitHub Pages
```

---

## 🛠️ 故障排查

### GitHub Pages 部署失败

1. 检查 **Actions** 标签页查看构建日志
2. 确保仓库名称为 `yyc3-ai-family`
3. 确保 **Pages** 设置中 Source 为 **GitHub Actions**

### 本地端口冲突

```bash
# 检查端口占用
lsof -i :3113

# 终止进程
kill -9 <PID>
```

### 环境变量问题

确保 `.env.production` 文件存在且格式正确。

---

## 📝 部署后配置

### API 密钥配置

部署后，在应用的 **Settings** 中配置：
- OpenAI API Key
- Anthropic API Key
- DeepSeek API Key
- 其他 LLM 提供商密钥

### 持久化策略

- **GitHub Pages**: 使用 `localStorage`（浏览器存储）
- **本地部署**: 可使用 `nasSqlite`（NAS 持久化）

---

**更新日期**: 2026-02-17
**版本**: 0.33.0
